# Base URL of the backend API (must end with /api).
# Local dev:
VITE_API_URL=http://localhost:5000/api
# Production (set this in Vercel project env vars), e.g.:
# VITE_API_URL=https://your-backend.vercel.app/api
